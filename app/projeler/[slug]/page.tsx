import type { Metadata } from 'next';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { ArrowLeft, ArrowRight, CheckCircle2 } from 'lucide-react';
import { caseStudies } from '../../content';
export function generateStaticParams(){return caseStudies.map(p=>({slug:p.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,p=caseStudies.find(x=>x.slug===slug);if(!p)return{};return{title:`${p.title} | Proje | JOKER YAZILIM`,description:p.summary,alternates:{canonical:`/projeler/${p.slug}`},openGraph:{title:`${p.title} | JOKER YAZILIM`,description:p.summary,type:'article'}};}
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params,p=caseStudies.find(x=>x.slug===slug);if(!p)notFound();const ld={'@context':'https://schema.org','@type':'CreativeWork',name:p.title,description:p.summary,creator:{'@type':'Organization',name:'JOKER YAZILIM'},keywords:p.tech.join(', ')};return <main className="seo-page"><header className="seo-nav"><Link href="/" className="seo-brand"><span>J</span> JOKER <b>YAZILIM</b></Link><Link href="/#iletisim" className="seo-cta">Proje Konuşalım <ArrowRight size={15}/></Link></header><article className="article-wrap"><Link href="/projeler" className="back-link"><ArrowLeft size={15}/> Tüm projeler</Link><span className="seo-eyebrow">{p.category} · {p.status}</span><h1>{p.title}<span>.</span></h1><p className="seo-lead">{p.summary}</p><div className="detail-layout"><section className="detail-main"><h2>Problem</h2><p>{p.problem}</p><h2>Çözüm</h2><p>{p.solution}</p><h2>Teknolojiler</h2><div className="check-list">{p.tech.map(x=><div key={x}><CheckCircle2 size={18}/><span>{x}</span></div>)}</div><h2>Mimari yaklaşım</h2><div className="check-list">{p.architecture.map(x=><div key={x}><CheckCircle2 size={18}/><span>{x}</span></div>)}</div><h2>Notlar</h2>{p.notes.map(x=><p key={x}>{x}</p>)}</section><aside className="detail-side"><small>JOKER / CASE STUDY</small><strong>Teknoloji<br/>ihtiyaca göre.</strong><p>Benzer bir sistemi işletmeniz için planlamak istiyorsanız kapsamı birlikte çıkarabiliriz.</p><Link href="/#iletisim" className="seo-cta wide">Teklif Al <ArrowRight size={16}/></Link></aside></div></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(ld)}}/></main>}
